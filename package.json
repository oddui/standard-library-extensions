{
  "name": "standard-library-extensions",
  "version": "0.1.1",
  "description": "JavaScript standard library extensions for building complex and scalable web applications.",
  "repository": {
    "type": "git",
    "url": "https://github.com/oddui/standard-library-extensions"
  },
  "scripts": {
    "ts:es": "tsc",
    "ts:defs": "tsc --declaration --outDir . --emitDeclarationOnly",
    "build": "npm run ts:es && npm run ts:defs",
    "clean": "rimraf index.d.ts helper util ui dist coverage",
    "prepublishOnly": "npm run test && npm run build",
    "postpublish": "npm run clean",
    "test": "jest --config jestconfig.json"
  },
  "author": "Ziyu",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "index.d.ts",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "jest": "^26.6.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.0",
    "typescript": "^4.1.3"
  }
}
